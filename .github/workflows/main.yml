name: gatsby-remark-responsive-iframe

on:
  push:
    branches:
      - main
jobs:
  install-gatsby:
    runs-on: ubuntu-latest
    steps:
      - name: install gatsby-remark-responsive-iframe
        run: npm install gatsby-remark-responsive-iframe
